# 디지털 기초 수업 실습 앱 (digitalbasic)

발달장애 성인 대상 **인공지능 수업**의 실습 웹앱.
본론은 3차시(AI가 배우는 방법)이고, 1·2차시는 학습자가 컴퓨터 조작 자체를 어려워해서
앞에 깔아 둔 준비운동이다 — 이 순서를 뒤집지 말 것.

1차시: 전원 켜고 끄기 · 마우스 · 키보드. 2차시: 키보드 위치 찾기 · 따라 쓰기.
3차시: 지도학습 / 비지도학습 체험. 4차시: AI와 대화 (대화창 사용법 + 낱말로 답 고르는 원리).
5차시: AI에게 그림 부탁하기 (낱말 카드 → 복사 → 붙여넣기 → 진짜 제미나이).
       2칸으로 익히고 → 3칸(어떤 + 무엇을 + 해 줘)으로 늘려 자세히 말하기까지 한다.

## 실제 수업 조건 (설계를 좌우한다)

**파일 번호 ≠ 실제 회차.** 1차시에 lesson1, **2차시에 lesson2+3+4를 몰아서** 했다.
그래서 lesson5는 **실제로는 3차시**다. 앞으로도 어긋날 수 있으니 "몇 차시"를 말할 땐 확인할 것.

- 회차당 **90~100분**. 학생 **8명**, 어른 **5명**(선생님 1 + 보조 4 = 학생 2명당 1명).
- **읽기 수준이 학생마다 크게 다르다.** 못 읽는 학생도 있다 → 낱말은 **이모지 + 글자 + TTS** 셋을 함께.
- **최종 목표는 실제로 인쇄되는 책 만들기.** AI로 받은 그림이 그 책에 들어간다.
- **타이핑은 앞으로도 안 될 것으로 본다** → 3차시(lesson5)부터 **12차시까지 그림카드가 기본 입력**.
  프롬프트는 **칸을 하나씩 늘리며** 자란다(2칸 → 3칸 → 4칸 → 주인공 5칸).
  다음 차시엔 **주인공 만들기**(머리·옷·악세사리·성격을 ◀▶로 넘겨 고르고, SVG 사람으로 보여줌).
  카드와 슬라이드는 「칸 + 후보」로 **같은 구조**다 — 슬롯을 그대로 두고 고르는 방식만 분기한다.
- **구글 계정이 학생들에게 없다.** 가입은 선생님이 **실시간으로 직접** 안내한다(30분+, 휴대폰 저장까지).
  **앱에 가입 안내를 넣지 않는다** — 구글이 UI를 자주 바꿔 앱 안내가 틀려지면 오히려 방해가 된다.
- 어른이 옆에 붙어 있으므로 **앱이 학생을 혼자 끌고 갈 필요가 없다.**
  앱이 못 하는 일(가입, 진짜 AI 결과 확인)은 사람이 맡는다.

## 핵심 파일

- `index.html` — 차시 선택 화면 (1~5차시 카드). 리다이렉트 아님.
  ⚠️ 카드가 늘면 `@media (max-height:860px)`의 `.card` 폭을 다시 계산할 것
  (`flex-wrap:nowrap`이라 1024폭에서 넘친다). 지금은 5장 기준 176px.
- `lesson1.html` — 1차시 (단일 자체완결 HTML, 인라인 CSS/JS/SVG).
- `lesson2.html` — 2차시. lesson1의 셸(헤더·TTS·볼륨·진행 점·칭찬·축하)을 복사해 만들었고, 스텝 타입은 `info`/`findkey`(위치 찾기)/`typeword`(따라 쓰기) 세 개.
  - `typeword`: 목표는 완성형(`도리`)으로 보여주고 입력칸은 실시간 조합(`composeHangul`). 틀린 자모도 **입력을 허용**해 빨갛게 남기고, 지우기로 바로잡아야 완성된다(`S.typed` 배열이 상태).
  - **단어 추가 시 금지**: 복합모음(ㅘㅙㅚㅝㅞㅟㅢ)과 된소리(ㄲㄸㅃㅆㅉ)·겹받침. 화면 키보드에 없고 Shift도 안 쓰므로 못 친다. `사과`(ㅘ)·`코끼리`(ㄲ)는 조합이 깨진다. 단자음+단모음만 쓸 것 — `고양이`·`대한민국`은 정상. 파트(모듈) 2개: '키보드 위치 찾기'·'글자 따라 쓰기'. 내부 홈(카드 선택) 화면에 index.html로 나가는 '다른 수업 고르기' 링크.
- `lesson3.html` — 3차시. 원안은 `archive/웹 실습 페이지 구현 계획.docx`. 스텝 타입 5개:
  `info`/`pick`(지도학습)/`sort`(비지도학습)/`compare`/`feature`/`recap`. 파트 3개.
  - 사진은 **두 세트**다. `PHOTOS_SUP`(`images/ai01~08.jpg`, 강아지·고양이)는 지도학습용,
    `PHOTOS_UNSUP`(`images/un01~08.jpg`, 토끼·햄스터)는 비지도학습용.
    **같은 사진을 재사용하지 말 것** — 지루하기도 하지만, 더 나쁜 건 학습자가 앞 단계 답
    ("강아지 여기, 고양이 저기")을 그대로 되풀이해서 '비슷한 점 찾기'를 안 하게 된다는 점이다.
  - 두 세트 모두 **종류와 털색(밝음/어두움)을 일부러 교차**시켰다.
    한쪽으로만 깔끔하게 나뉘게 바꾸면 비지도학습이 '정답 맞추기'로 변질되고 `feature` 질문이 무의미해진다.
  - 비지도 세트는 정답이 새지 않아야 한다: `alt`는 "동물 사진 N", 이름표 빈 칸,
    파일명도 중립(`un01`, `rabbit01` 아님). 스크린리더로도 종류를 알 수 없어야 한다.
  - 사진이 없으면 `onerror`로 이모지 카드 대체 → 이미지 없이도 실습이 끝까지 돌아간다.
  - `sort`: **끌어다 놓기 + 누르기 둘 다** 지원. 드래그는 발달장애 학습자에게 어려우니
    누르기(그룹 버튼 메뉴)를 절대 없애지 말 것. 12px 미만 움직임은 클릭으로 처리(손떨림).
  - 원안 중 **일부러 안 따른 것**: 파일 3분할(→단일 HTML), '올바르게 선택한 수' 표시(→점수 금지),
    숫자 단축키·Esc(→인지 부담). 원안의 "지체장애인" 대상 서술도 이 수업 실제 대상과 다르다.
- `lesson4.html` — 4차시. 스텝 타입 3개: `info`/`send`(대화)/`recap`. 파트 3개.
  - **API를 쓰지 않는다 (되돌리지 말 것).** 학생들이 각자 노트북으로 접속하고 이 앱은
    GitHub Pages 정적 사이트다. API 키를 넣으면 F12로 노출되고 소스도 공개라
    누구나 키를 가져가 **선생님 계정에 요금이 청구된다.** 조심해서 될 문제가 아니다.
    게다가 진짜 AI는 속을 볼 수 없어서 「원리 가르치기」라는 목표 자체를 못 이룬다.
  - 답변은 `RULES` 배열의 **단순 부분 문자열 검색**. 일부러 단순하게 뒀다 — 복잡하면 원리가 안 보인다.
  - **대화가 이어져야 한다.** 규칙에 `ask`(되물음)·`answers`(대답 카드)·`follow`(마무리)·
    `topic`(설명용 짧은 말)을 두고, AI가 되물으면 `S.pending`이 세팅되어
    **다음 차례 카드가 그 질문의 대답으로 바뀐다.** 되물어 놓고 같은 말만 반복하면
    대화가 아니라 자판기가 된다(처음에 그렇게 만들었다가 고쳤다).
  - `S.pending`이 있을 땐 `matchRule`을 **건너뛴다.** 안 그러면 '더운 날이 좋아요'가
    `좋아`(mood 규칙)에 걸려 날씨 대화가 끊긴다.
  - 같은 주제를 또 꺼내면 `AGAIN` 문구를 앞에 붙여 표현을 바꾼다(`S.said`로 기억).
  - `send`의 `mode`가 두 개: `cards`(문장 카드 고르기)·`fill`(빈칸에 낱말 직접 치기).
    **'쓰기 → 보내기'가 두 동작임을 익히는 게 1파트의 핵심** — 카드를 눌러도 전송되지 않는다.
  - `reveal:true`면 보낸 말에서 걸린 낱말을 노랗게 표시하고 무엇을 봤는지 설명한다(2파트 핵심).
  - **`<input>`을 쓰지 않는다.** 2차시처럼 화면 자모 키보드 + `composeHangul`로 조립한다.
    진짜 input을 쓰면 한/영이 영문일 때 `날씨` 대신 `skfTl`가 찍히고 학생은 이유도 모른 채 막힌다.
  - **`fill`의 `word`·카드 문장을 바꿀 때 반드시 확인할 것**:
    ① `word`는 화면 키보드에 있는 자모만 — 된소리(ㅆ)·복합모음(ㅘ) 금지.
       `날씨`로 뒀다가 ㅆ에서 막히는 걸 실제로 겪었다(`노래`로 교체).
    ② `unknown` 파트 카드는 `RULES`의 어떤 낱말도 들어 있으면 안 된다.
       `우리 집 강아지 이름은?`이 `이름` 규칙에 걸려 AI가 아는 척 답했다.
- `lesson5.html` — 5차시(**실제 수업으로는 3차시** — 위 '실제 수업 조건' 참고).
  스텝 타입 5개: `info`/`build`/`pick`/`real`/`recap`.
  **파트 4개**('연습해 보기' 2칸 · '자세히 말하기' 3칸 · '진짜 AI에게 부탁하기' 3칸 · '많이 부탁해 보기').
  - **「많이 부탁해 보기」가 마지막**이다. 진짜 AI를 한 번 겪은 뒤에 와야
    '이걸 붙여넣으면 그림이 나온다'를 이미 알아서 목록을 계속 퍼다 쓸 이유가 생긴다.
  - **「많이 부탁해 보기」는 골라 쓰는 파트**다. `pick` 스텝이 이미 만들어진 문장 30개를
    길게 늘어놓고 줄마다 복사 버튼을 둔다(`PICK_LINES`). 낱말 조립이 힘든 학생도 바로 쓴다.
    - **여기서만 복사를 잠그지 않는다.** 다른 스텝은 한 번 복사하면 끝이지만
      이 화면은 '계속 복사해서 쓰는' 것이 목적이라 몇 번이든 다시 누른다.
      한 번 복사하면 「다음」이 열리고, 그 뒤로도 계속 복사할 수 있다.
    - `PICK_LINES`는 **손으로 적지 않고 `PICK_PAIRS`(낱말 짝)에서 만들어 낸다** —
      낱말을 고치면 목록도 따라 바뀌고, 없는 낱말이 섞일 일이 없다.
    - 목록은 **`.swap`이 스크롤되는 게 정상**이다. 넘침 검사에서 여기만 예외로 둘 것
      (`.picklist`가 있으면 건너뛴다). 대신 「다음」 버튼은 늘 화면에 남아야 한다.
  - **`step.back:true`면 「◀ 앞으로」 버튼이 나온다**(`prevStep`). 이 파트에만 붙였다 —
    「골라 쓰기 ↔ 내가 만들기」를 오가려는 것이고, 한 화면에 둘을 넣으면 1024×600에 안 들어간다.
    되돌아갈 때는 **칭찬을 띄우지 않는다**(뒤로 가는 건 잘한 일도 못한 일도 아니다).
    다른 파트는 지금까지처럼 앞으로만 간다.
  - 「내 책 만들기」 파트는 **일부러 뺐다.** 책 이야기는 아직 이르다 —
    이번 시간은 복사·붙여넣기와 진짜 AI 체험까지만 한다.
  - **시간이 모자라면 「자세히 말하기」를 먼저 덜어낸다.** 90분에 4파트는 빠듯할 수 있는데
    홈에서 파트를 골라 들어가므로 그냥 건너뛰면 된다(각 파트는 혼자서도 돌아간다).
    「진짜 AI」가 이 차시의 핵심이고, 「많이 부탁해 보기」는 낱말 조립이 힘든 학생에게
    특히 쓸모 있으니 이 둘을 남길 것.
  - **갈래(`GROUPS`) 구조.** 갈래를 누르면 그 안의 낱말만 보인다. 갈래 10개:
    「어떤」용 4개(기분·크기·움직임·쉬기) + 「무엇을」용 6개(동물·하늘바다·먹을것·자연·탈것·물건).
    갈래의 `role`이 **그 낱말이 들어갈 칸**을 정한다(안 적으면 `what`).
    **세 파트가 똑같은 방식**이다 — 칸이 하나 늘 뿐 학생이 새로 배울 조작이 없다.
  - **색깔은 일부러 뺐다.** 「빨간 고양이」는 결과가 뻔해서 그림이 크게 안 바뀐다.
    모습·움직임은 그림이 확 달라져 **"내 말이 그림을 바꿨다"가 훨씬 잘 보인다.**
    그 자리에 모습·움직임을 각각 둘로 나눠 넣었다(18개 → 24개).
  - **갈래마다 낱말 6개까지.** 열 안에서 2개씩 3줄로 깔려 딱 맞는다.
    **7개를 넣으면 4줄이 되어 1024×600에서 40px 넘친다**(실제로 재 봤다).
    낱말을 늘리려면 갈래를 나눌 것 — 갈래는 한 열에 4개까지 한 줄에 들어간다.
  - 갈래 아이콘은 **선이 얇은 것(📐 같은 것)을 피한다** — 작은 탭에서 안 보인다.
  - **카드를 늘리면 `REPLIES`에도 같은 낱말을 넣을 것** — 없으면 🙂(`DEFAULT_REPLY`)로 떨어진다.
    단 **「어떤」 낱말은 `REPLIES`에 넣지 않는다**(18×36=648줄이 된다).
    `replyFor`가 고른 말을 답 문장 앞에 붙인다 — "네! 점프하는 곰 그림이에요."
    「어떤」을 골라도 답이 똑같으면 그 칸을 왜 채웠는지 알 수 없어서 이렇게 했다.
  - **칸마다 세로 열을 하나씩 준다**(`.slotcols` / `.slotcol`) — 어떤 | 무엇을 | 해 줘.
    탭과 낱말이 같은 열에 있어 '이 탭은 어느 칸용인지'가 자리로 보이고,
    탭이 두 줄로 쌓여도 **열 안에서만 쌓여 옆 칸을 침범하지 않는다.**
    ⚠️ 처음에 탭을 한 줄에 다 늘어놓았다가 **줄바꿈된 탭이 낱말 카드를 덮어썼다.**
    2칸 스텝은 열이 저절로 2개가 된다 — 코드는 그대로다.
  - `S.gi`가 지금 보는 갈래(그 스텝이 쓰는 갈래 안에서의 번호).
    **갈래를 바꿔도 이미 고른 낱말은 그대로 남는다**(`S.filled`와 별개).
    한 열의 탭을 눌러도 **다른 열은 안 바뀐다**(`shownFor`가 열마다 따로 고른다).
    스텝의 `group`은 '처음 열어 둘 갈래'일 뿐 — 학생은 언제든 바꿀 수 있고, `cards`는 전부 넘긴다.
  - `.buildwrap`은 **1180px**이다(940 아님). 문장칸·트레이만 940으로 따로 묶어 뒀다.
    카드를 음수 마진으로 넓히려다 **1366에서 마지막 카드가 잘리고 가로 스크롤이 생긴 적이 있다**
    — 컨테이너 폭으로 풀 것.
  - **`.buildwrap.picking`** — 낱말을 고르는 동안(복사 전)에만 붙는 클래스.
    아직 못 쓰는 것들(빈 트레이·복사·다음 버튼)을 낮춰 **카드 6장이 다 보이게** 한다.
    복사한 뒤엔 원래 크기로 돌아온다. 3열이 되면서 카드가 한두 장만 보이던 걸 이걸로 풀었다.
    **860·700 두 블록에 다 넣어야 한다** — 700에만 넣었다가 1366×768에서 카드가 잘렸다.
  - **타이핑을 아예 쓰지 않는다 (되돌리지 말 것).** 학생들이 문장 치기를 힘들어해서
    **낱말 카드를 눌러 조립**한다. 화면 자모 키보드·`composeHangul`을 통째로 뺐다.
  - **`slots` 개수를 코드에 박지 말 것.** `sentenceText`/`isComplete`는 `slots` 배열을 그냥 훑는다.
    지금 2칸(`SLOTS2`)·3칸(`SLOTS3`)이 한 파일에서 같이 돈다 — **칸을 늘릴 땐 배열만 추가**하면 되고
    러너·CSS는 안 건드린다(3칸 추가할 때 실제로 그렇게 됐다. 레이아웃만 손봤다).
    다음은 4칸 → 주인공 만들기 5칸.
  - 카드를 **아무 순서로 눌러도** `role`이 자리를 정한다 — 순서 때문에 틀리는 일이 없다.
  - 카드는 **이모지 + 글자 + TTS** 셋을 함께 준다. 학생마다 읽기 차이가 커서
    글자만 두면 못 읽는 학생이 막히고, 이모지만 두면 읽는 학생이 배울 게 없다.
  - **`navigator.clipboard.readText()`를 쓰지 않는다 (되돌리지 말 것).** 크롬이 권한 창을 띄우는데
    학생이 '차단'을 누르면 되돌리는 길이 주소창 자물쇠뿐이라 그 노트북이 그대로 막힌다.
    붙여넣기는 JS 변수 `S.clip`을 읽는다. **복사는 진짜로 한다** — 마지막 파트에서 진짜 제미나이에 붙여야 하고,
    연습만 가짜면 연습이 실물과 달라져 막상 제미나이 앞에서 처음 하는 일이 된다.
  - **복사 실패를 화면에 표시하지 않는다.** 실패 상태가 생긴다 — 콘솔 `[clip]` 로그만 남긴다.
    `execCopy` 폴백의 숨김 `<span>`에 `user-select:text`가 **필수**다
    (body의 `user-select:none`이 상속되면 선택이 비어 복사가 실패한다).
  - 복사는 눌러도 아무 일이 없어 보이는 게 제일 어려운 점이라 **클립보드 트레이**를 뒀다.
    상태는 애니메이션이 아니라 **클래스(`.tray.full`)로 남긴다** — 모션을 꺼도 담긴 게 보여야 한다.
  - **붙여넣기는 「마우스 오른쪽 → 붙여넣기」가 주된 길이다 (되돌리지 말 것).**
    `Ctrl+V`만 알려 주면 안 된다 — **학생들은 알파벳을 못 읽는다.**
    V가 어느 키인지 알 길이 없으니 「Ctrl+V를 누르세요」는 아무 안내도 아니다.
    오른쪽 버튼 메뉴는 **글자가 한글**이고, **마우스는 1차시에서 이미 배웠다.**
    `pasteHow()`가 마우스 그림(오른쪽 버튼만 노랑) + 메뉴 그림(「붙여넣기」만 노랑)을 그린다.
    `Ctrl+V`는 아래 한 줄로 곁들인다(아는 학생에게는 그게 더 빠르다) — 없애지는 말 것.
    ⚠️ 처음에 키보드 그림에 Z X C **V**를 그렸다가 이 문제를 깨닫고 갈아엎었다.
    `.gstep b.rb`는 문장 속 강조용이다 — 안 두면 번호 동그라미 스타일을 물려받아 파란 원이 된다.
  - 마지막 파트(진짜 AI)는 제미나이로 **새 탭**을 연다(`rel="noopener noreferrer"` 필수).
    **`visibilitychange`로 자동 진행하지 말 것** — 실수로 탭만 바꿔도 진도가 넘어간다.
    앱은 붙여넣었는지 확인할 수 없다. 「👍 해봤어요」 버튼 **하나만** 둔다 —
    「못 했어요」를 두면 그게 곧 실패 상태다.
  - 레이아웃: `.swap` 한 칸을 **낱말카드 ↔ AI창(또는 제미나이 안내)이 번갈아 쓴다.**
    동시에 두면 1024×600에 안 들어간다. 낱말카드일 땐 **열 안(`.colcards`)에서만 스크롤**하고,
    AI창일 땐 `.swap`이 스크롤한다(`.swap:has(.slotcols)`로 가른다).
    `@media (max-height:700px)`가 1024×600용이고 **860 블록의 값 자체는 그대로 두었다**
    (760으로 바꿨다가 1366×768이 깨진 그 버그 — 값을 고치지 말고 규칙을 더할 것).
- `archive/` — 폐기·참고용 옛 자료(`마우스배우기.html`, 옛 이미지, 3차시 원안 `.docx`).
  **학생 앱과 분리**되어 있고 GitHub Pages로 배포되지 않는다. `.docx`만 git에 추적하고
  무거운 옛 에셋(jpg·png·svg·html)은 `.gitignore`로 제외한다. 여기 파일은 수정하지 말 것.

## 폴더 구조

```
digitalbasic/
├─ index.html            차시 선택 화면 (GitHub Pages 진입점 — 루트 고정)
├─ lesson1~5.html        차시별 단일 자체완결 파일
├─ images/               3차시 실습 사진 (ai01~08 지도학습 · un01~08 비지도학습)
├─ CLAUDE.md · README.md
└─ archive/              옛 자료 (배포 제외)
```

**`index.html`과 `lesson*.html`은 루트에 있어야 한다** — GitHub Pages가 루트에서 서빙하므로
하위 폴더로 옮기면 학생 주소(`.../digitalbasic/`)가 깨진다. 링크는 모두 상대경로다.

각 차시는 독립된 자체완결 파일. 셸 코드가 중복되지만 의도된 선택 — 한 차시 수정이 다른 차시를 깨뜨리지 않는다.
(3차시만 `images/`를 쓴다. 사진은 400px·JPEG로 줄여 16장 합계 232KB — 원본 1024px PNG는 장당 1.3MB라 수업 노트북에 무겁다.)

## 저장소·배포 (중요)

- GitHub: **https://github.com/HYUNMINJU/digitalbasic** (main 브랜치)
- 학생 접속 주소(GitHub Pages, 기본): **https://hyunminju.github.io/digitalbasic/**
  - 배포 방법: main에 커밋 후 push하면 자동 배포 (1분 내외). `index.html`이 차시 선택 화면.
- 보조 주소(Artifact): https://claude.ai/code/artifact/c25ca8eb-fbd2-4ba9-938a-86858f291d6a
  - 재게시 시: lesson1.html에서 `<!doctype html>`/`<html>`/`<head>`/`<meta>`/`<body>` 계열 래퍼 태그 줄만 제거한 사본을 Artifact 도구로 게시하되, **반드시 `url` 파라미터에 위 주소를 넘겨 같은 주소 유지**.
- 수정 완료 시 커밋·push까지 해야 학생 주소에 반영됨.

## 코딩 규칙

- ponytail 플러그인 스타일: 외부 라이브러리 0, 브라우저 기본 기능만 (Web Speech API TTS, localStorage, CSS 애니메이션). 최소 코드.
- 구조: `MODULES` 배열(단일 데이터 소스) + `RUN` 스텝 타입 디스패처. 화면은 `#stage` 하나를 innerHTML로 재렌더.
- 새 연습 추가 = MODULES에 스텝 객체 추가 + (새 타입이면) RUN에 러너 함수 추가.

## 접근성 원칙 (변경 금지)

- 실패 상태 없음: "틀렸어요" 금지, 오입력은 부드러운 안내 음성만
- 한 화면 한 행동, 큰 타깃(88px+), 큰 글씨, 고대비 밝은 테마(빔프로젝터용)
- 모든 지시문 TTS 자동 낭독(15자 내외) + 헤더 소리 조절(🔊/🔉/🔇)·다시 듣기
- 시간 압박 없음, 타깃 위치 랜덤 배제, 성공마다 칭찬
- 키보드 판정은 `e.key`가 아닌 **`e.code`** 기준 (한글 IME 대응)

## 로컬 테스트

- Playwright MCP는 file:// 차단 → `python -m http.server 8737` 후 `http://localhost:8737/lesson1.html`
- TTS는 헤드리스에서 안 들림 → 콘솔 `[tts]` 로그로 문구 확인
- 확인 크기: 1920×1080(프로젝터) / 1366×768(수업 노트북) / 1024×600
  - **1024×600이 관문**: 2차시 화면 키보드는 5행(숫자행 포함)이라 세로가 빠듯하다. 키보드 하단이 잘리지 않는지 반드시 확인.
  - **1366×768도 같이 볼 것**: 3차시 CSS breakpoint가 `max-height:860px`인 이유다.
    760으로 두면 768 화면이 스타일을 못 받아 그냥 넘친다(실제로 겪은 버그).
  - 3차시는 `sort` 화면이 제일 빡빡하다(사진 8 + 그룹칸 2 + 버튼). 넘침 확인은 이렇게:
    `document.querySelector('main#stage')`의 `scrollHeight - clientHeight`가 0이어야 한다.
  - 4차시는 **대화 기록이 길어져도 입력칸·보내기·키보드가 화면에 남아야** 한다.
    `.chatlog`만 `overflow-y:auto`로 스크롤된다. 말풍선을 10개쯤 넣고 확인할 것.
  - 5차시는 **화면 상태가 4개**라 각각 봐야 한다
    (낱말카드 / 문장 목록 / 붙여넣은 뒤 AI창 / 제미나이 안내).
    복사·다음 버튼은 항상 남아야 하고, 낱말카드일 땐 `.colcards`(열)가 안 넘쳐야 한다.
    **문장 목록(`.picklist`)만은 스크롤이 정상**이다 — 넘침 검사에서 예외로 둘 것.
    1024×600에서 **제미나이 안내 화면이 제일 빡빡하다**
    (안내 2줄 + 마우스 그림 + Ctrl+V 줄 + 제미나이 버튼 + 해봤어요).
    `main#stage:has(#guide)`로 그때만 트레이·복사 버튼을 낮춰 자리를 냈다.
  - 홈 카드가 **4장**이다. 파트가 더 늘면 `@media (max-height:860px)`의 `.card` 폭을
    다시 계산할 것 — 두 줄이 되면 아래 '다른 수업 고르기' 링크가 잘린다.
    한 줄로 미는 `nowrap`은 `(min-width:821px)`을 함께 걸어 좁은 화면은 그대로 둔다.
    **가로도 볼 것**: `document.documentElement`의 `scrollWidth - clientWidth`가 0이어야 한다.
    세로만 재다가 1366에서 카드가 잘린 걸 못 본 적이 있다 — **눈으로도 볼 것**.
  - ⚠️ **한 화면만 재고 끝내지 말 것.** 갈래마다 낱말 길이가 달라서
    (`자는`은 두 글자, `누워있는`은 네 글자) **고른 갈래에 따라 넘침이 달라진다.**
    짧은 낱말 탭만 재고 통과시켰다가 긴 낱말 탭에서 카드가 덮인 걸 놓쳤다. 전부 훑을 것:
    ```js
    // 모든 스텝
    for(let m=0;m<MODULES.length;m++) for(let s=0;s<MODULES[m].steps.length;s++){
      S.mi=m; S.si=s; runStep(); /* stage·colcards·documentElement 넘침 확인 */ }
    // 모든 갈래 탭
    document.querySelectorAll('.gtab').forEach(t => { t.click(); /* 같은 확인 */ });
    ```
- 파일을 고쳐도 브라우저가 옛 파일을 물고 있는 일이 잦다 → **`?v=숫자`를 붙여** 확인.
- 진행 기록: localStorage `digibasic_l1_v1`~`digibasic_l5_v1`(차시별 모듈 완료), `digibasic_vol`(소리 설정, 차시 공유)
- 5차시 복사 확인: 콘솔 `[clip] ok`(또는 `fail`) 로그. 메모장에 Ctrl+V 해서 실제로 들어가는지 볼 것.
  붙여넣기를 눌렀을 때 **클립보드 권한 창이 뜨면 안 된다** — 뜨면 `readText`가 섞여 들어간 것이다.
